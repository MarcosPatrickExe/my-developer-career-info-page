import { Routes } from '@angular/router';

// A landing page é single-page (storytelling por scroll), então mantemos
// uma única rota por enquanto. Estrutura pronta para crescer, ex:
// { path: 'projetos/:slug', loadComponent: () => import(...) } para
// páginas de case study detalhadas no futuro.
export const routes: Routes = [];
